name: home-media
channels:
  - conda-forge
dependencies:
  # Python
  - python=3.11.9

  # Core dependencies
  - pyyaml=6.0.1
  - pandas=2.1.4
  - pillow=10.2.0
  - exifread=3.0.0

  # Jupyter and notebooks
  - jupyter=1.0.0
  - notebook=7.0.6
  - ipykernel=6.28.0
  - ipython=8.20.0

  # Development tools
  - pip=25.3

  # Testing
  - pytest
  - pytest-cov
  - pytest-mock
